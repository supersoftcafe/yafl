
# Struct with inferred types

fun `+`(a, b) = __builtin__ add_i32(a, b)
fun `+`(a, b) = __builtin__ add_i64(a, b)

struct NumberPair(left, right)

val wonder = 27
val value = NumberPair(wonder, 8)

fun main() = value.left + value.right
