
# More + operators to test correct resolution and a conditional with boolean

fun `+`(a, b) = __builtin__ add_i32(a, b)
fun `+`(a, b) = __builtin__ add_i64(a, b)

fun addFour(a, b) = a ? b + 4 : b + 22

let value = 27 + 8

fun main() = addFour(false, value)
