# Changelog

## 0.1.0

- Initial release: TextMate grammar, language configuration, and snippets for
  YAFL. Replaces the old `yafl.tmbundle/` at the repository root; grammar
  reconciled with `compiler/parsing/tokenizer.py`.
