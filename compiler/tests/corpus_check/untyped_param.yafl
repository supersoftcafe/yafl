# check-contract corpus: a parameter no USE in the body determines — the body
# is the only evidence there is, so this is uninferable and reported with its
# clue before the check phase proper.
typealias Int : __builtin_type__<bigint>

fun orphan(v): Int
  ret 0
